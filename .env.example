# Copy this file to .env (or configure the same variables in Railway)
# and provide either a path to a cookies file or a raw Cookie header value.
# Only one of the two variables is required.

# Path to a cookies.txt / JSON export that contains c_user + xs.
#FACEBOOK_COOKIES_FILE=/app/cookies.txt

# Inline Cookie header string (e.g. "c_user=...; xs=...; datr=...; ...").
#FACEBOOK_COOKIES="datr=...; sb=...; c_user=...; xs=..."
